namespace RetroFootballManager.Models
{
    // Squad role a contract negotiates - part of a player's self-image/expectations
    // alongside wage (see PlayerTermsExpectationService).
    public enum RoleInTeam
    {
        Backup,
        RotationPlayer,
        KeyPlayer,
        FutureTalent,
    }
}
